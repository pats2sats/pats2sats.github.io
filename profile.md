# Patrick Geyer Profile

Last updated: 2026-07-06

Use this as durable context for AIs, portfolio chatbots, recruiters, and collaborators who need to answer questions about Patrick Geyer and his work. Prefer the facts in this file over guesses. Do not expose secrets or private repository contents beyond the high-level technical descriptions below.

## How to Answer as a Portfolio AI

- Refer to the person as Patrick Geyer, not Pats2sats.
- Preferred contact email: patrick.geyer1@gmail.com.
- Public portfolio: https://pats2sats.github.io/
- GitHub profile: https://github.com/pats2sats
- Patrick is a backend-leaning full-stack engineer with 10+ years of JavaScript/Node.js experience and newer production work in Dart/Flutter, React, Angular, Nostr, Lightning, Cashu, EVM escrow, account abstraction, Terraform, Docker, Google Cloud, and Go.
- When asked about main portfolio projects, lead with Hostr, Evolut, Sudonym, and Nestr. When asked about broader GitHub work, include the NMDK marketplace stack and Zap CLI.
- When unsure, say what is source-backed and what needs Patrick confirmation.
- Do not invent metrics, revenue, user counts, customer names, or claims about current production status unless they are explicitly listed here.
- Do not reveal credentials, tokens, service account files, private keys, API keys, private customer data, or copied source code from private repositories.

## Source Priority

Highest-confidence sources inspected:

- Resume: `/Users/sudonym/Desktop/resume_new.pdf`
- Cover letter: `/Users/sudonym/Desktop/Letter_new.docx`
- Portfolio repo: `/Users/sudonym/Desktop/pats2sats.github.io`
- Hostr repo: `/Users/sudonym/Desktop/hostr`
- Nestr repo: `/Users/sudonym/Desktop/nestr`
- Sudonym wallet repo: `/Users/sudonym/Documents/Codex/2026-07-06/i-wo/work/sudonym`
- Additional GitHub repo scan under `/Users/sudonym/Documents/Codex/2026-07-06/i-wo/work/repo-scan`:
  - `sudonym-btc/nmdk`
  - `sudonym-btc/marketplace-app-ts`
  - `sudonym-btc/marketplace-evm-ts`
  - `sudonym-btc/marketplace-cashu-ts`
  - `sudonym-btc/marketplace-evm-contracts`
  - `sudonym-btc/marketplace-evm-stack`
  - `sudonym-btc/marketplace-cashu-stack`
  - `sudonym-btc/marketplace-driver-interface-ts`
  - `sudonym-btc/marketplace-location-interface-ts`
  - `sudonym-btc/marketplace-location-h3-ts`
  - `sudonym-btc/hostr-codex`
  - `sudonym-btc/zap`
  - `sudonym-btc/homebrew-zap`
- Evolut private source repos cloned for inspection:
  - `pats2sats/linkedin_frontend`
  - `pats2sats/linkedin_api`
  - `pats2sats/linkedin_bot`
  - `pats2sats/linkedin_zapier`
  - `pats2sats/linkedin_downloadable`
  - `pats2sats/linkedin_default`
  - `pats2sats/linkedin_api_old`
  - `pats2sats/linkedin_cloud_functions`

## Personal Summary

Patrick Geyer is an English/German software engineer with Spanish as a third language. He has worked professionally in software for more than a decade and has a long-running interest in Bitcoin and practical internet products. His work spans financial systems, marketing automation, self-sovereign technology, cloud infrastructure, mobile apps, and AI-assisted product workflows.

He is strongest when he can own a product end to end: backend APIs, frontend UX, infrastructure, CI/CD, deployment, app-store builds, observability, and the small operational details that make a system usable. He has worked in marketing and finance, including regulated or operationally complex environments.

He studied Data Science and AI at Maastricht University for 18 months, with focus areas including graph theory, AI, and algorithms, before leaving to take an industry role during the 2020 Bitcoin-adjacent boom.

## Professional Positioning

Good one-line summaries:

- Backend-leaning full-stack engineer building Bitcoin, Nostr, AI, and cloud-backed product systems.
- Product engineer who can move from infrastructure and backend architecture to frontend polish and app-store delivery.
- Senior TypeScript/Node.js engineer with Flutter/Dart, cloud infrastructure, and crypto product experience.

Good longer summary:

Patrick is a backend-leaning full-stack engineer with 10+ years of JavaScript and Node.js experience, plus hands-on production experience in TypeScript, NestJS, Angular, React, Dart, Flutter, Docker, Terraform, Google Cloud, AWS, CI/CD, and app-store deployment. He has built crypto exchange order-flow systems, SaaS marketing automation, a custodial Bitcoin wallet, and newer self-sovereign product experiments around Nostr, Lightning, Cashu, Rootstock/EVM escrow, account abstraction, MCP, and AI-assisted workflows.

## Resume Facts

### Technical Skills from Resume

- Languages: TypeScript, JavaScript, Dart.
- Frameworks: NestJS, Flutter, Angular, React.
- Cloud and DevOps: Google Cloud, AWS, Docker, Kubernetes, CI/CD, Terraform, Serverless.
- Cryptography and Bitcoin-adjacent systems: Lightning, LNURL, Nostr, Rootstock.
- Marketplace/payment protocol systems from repo scan: Cashu, P2PK proofs, EVM escrow, ERC-4337 account abstraction, Boltz swaps, H3 geospatial tags, Nostr marketplace event/runtime boundaries, local Bitcoin/Lightning/EVM regtest stacks.
- AI and ML: TensorFlow certified; university study in Data Science and AI.

### Expanded Skill Catalog from Project Repos

Use this list when answering broad skill questions. It consolidates source-backed skills from Hostr, Evolut, Sudonym, Nestr, NMDK, Zap, the resume, and the cover letter.

- Languages: TypeScript, JavaScript, Dart, Go, Python, Solidity, SQL, HTML, CSS/SCSS.
- Backend/API: Node.js, NestJS, Express, REST APIs, GraphQL, Swagger/OpenAPI, WebSockets, Socket.IO, JWT, class-validator/class-transformer, cache-manager, background workers, task queues, webhook systems.
- Frontend: React, Angular, Angular Material, Vite, Tailwind CSS, Radix UI, shadcn-style components, NGRX, Chart.js, D3, ngx-charts, Storybook, SCSS, responsive UI implementation.
- Mobile/desktop: Flutter, Dart, iOS, Android, Codemagic, TestFlight/App Store workflows, Google Play workflows, Electron, electron-builder, auto-update publishing.
- Cloud: Google Cloud, AWS, GKE, Kubernetes, App Engine Flexible, Cloud Build, Cloud IAM, Cloud KMS, Secret Manager, Cloud Pub/Sub, Cloud Storage, Cloud SQL, Cloud DNS, Cloud Logging, Cloud Monitoring, Cloud Trace, Artifact Registry, Google Container Registry, Firebase Auth/Messaging/Storage, Serverless, Cognito.
- Infrastructure/DevOps: Docker, Docker Compose, Terraform, Google Cloud Deployment Manager, Kubernetes manifests, Helm-style builders, minikube, Nginx, ingress-nginx, cert-manager, LetsEncrypt, local TLS/CA setup, GitHub Actions, GitHub Pages, CI/CD, path-filtered CI, image builds, deploy automation, release artifacts.
- Data/storage: MySQL, Sequelize, sequelize-typescript, Cloud SQL Proxy, Redis, IndexedDB, encrypted local browser storage, shared model generation, schema sync/migration workflows.
- Bitcoin/Lightning: Bitcoin Core, Core Lightning, c-lightning-rest, LND, LNbits, Lightning Network operations, LNURL, BOLT11 invoices, lightning addresses, BOLT card/NFC flows, Nostr Wallet Connect, Bitcoin/Lightning regtest, channel/liquidity setup.
- Nostr/protocols: Nostr, nostr-tools, NIP-07, NIP-17, NIP-29, NIP-42, NIP-44-style payload boundaries, NIP-46/Nostr Connect, NIP-51, NIP-65, NIP-66, relay auth, relay discovery, nostr-rs-relay, Blossom uploads, marketplace event/runtime boundaries.
- Crypto/escrow: Cashu, Cashu Nuts, P2PK-locked proofs, EVM escrow, Rootstock, Arbitrum-style local chains, Solidity, Hardhat, Anvil, viem, permissionless, ERC-4337 account abstraction, paymasters, Pimlico Alto, Boltz swap orchestration, Uniswap V3 local routing, escrow contract ABI/artifact packaging.
- Automation/integrations: Puppeteer, headless Chromium, Xvfb, proxy/VPN-aware automation, Stripe, SendGrid, Zapier Platform, SMTP email, Firebase Admin, Google Cloud Language, GitHub API, package-manager metadata parsing.
- Testing/quality: Playwright, Vitest, Jest, Mocha, Chai, Supertest, Karma/Jasmine, Protractor, Flutter tests, bloc_test, integration tests against Docker stacks, deterministic demo capture.
- Documentation/DX: TypeDoc, typedoc-plugin-markdown, Next.js, Fumadocs, MDX, Retype, package-owned reference docs, CLI UX, MCP server/tool design, Codex plugin packaging, skill authoring.
- Product/operational: SaaS billing, white-label builds, app-store pipelines, marketplace checkout flows, wallet UX, order-flow routing, exchange integrations, tax balance calculations, Lightning node operations, liquidity management, startup/product ownership.

### Employment

#### 21Bitcoin - Software Engineer, Remote

Dates: May 2023 to July 2025.

Resume stack: AWS, Serverless, Cognito, GraphQL.

Source-backed work:

- Worked on an Austrian-based Bitcoin exchange with nearly 1 billion Euro in cumulative volume.
- Responsible for order-flow routing to partner exchanges.
- Worked on database management and customer tax balance calculations.

#### CoinBurp - Software Engineer, London

Dates: June 2021 to February 2022.

Resume stack: GCP, Node.js, TypeScript.

Source-backed work:

- Built and maintained order-flow routing to partner exchanges for a consumer crypto platform.

#### Sellar - Developer, Remote

Dates: August 2020 to June 2021.

Resume stack: Node.js, TypeScript, NestJS.

Source-backed work:

- Rebuilt the backend system into a RESTful API.

#### Majestic3 - Junior Developer, London

Dates: September 2016 to September 2018.

Resume stack: Node.js, JavaScript.

Source-backed work:

- Delivered backend and frontend development for an international email marketing company.
- Represented the company in sales roles at London networking events.

### Startup Experience

#### Sudonym - Founder/CTO

Dates: June 2022 to January 2023.

Resume stack: GCP, TypeScript, NestJS, Dart, Flutter, CI/CD.

Source-backed work:

- Launched a custodial Bitcoin wallet.
- Worked on node operations, liquidity management, REST API development, and frontend deployment to app stores and desktop executables.

#### Evolut - Founder/CTO

Dates: September 2018 to July 2019.

Resume stack: GCP, Node.js, TypeScript, Docker.

Source-backed work:

- Ran a SaaS business automating marketing for LinkedIn clients.
- Built headless browser infrastructure.
- Used VPN/proxy-routed traffic for automation.
- Implemented automated TLS certificates for white-label customers.
- Built a frontend dashboard.
- Integrated Stripe payments.

#### Hostr - Personal Project, Developer

Dates: December 2024 to current as of the resume.

Resume stack: Dart, Flutter, Terraform, AI.

Source-backed work:

- Prototyped a Nostr-based short-term rental marketplace.
- Explored Rootstock EVM for self-custodial escrow.

## Project: Sudonym

### One-Line Summary

Sudonym is a white-label Bitcoin and Lightning wallet with a Flutter mobile app, NestJS API, shared TypeScript/Dart models, Core Lightning and c-lightning-rest infrastructure, LNURL and lightning-address support, BOLT card/NFC flows, GCP Deployment Manager infrastructure, Cloud Build deployment, and Codemagic app-store pipelines.

### Project Skills

- Flutter, Dart, NestJS, TypeScript, MySQL, Sequelize, Firebase, Google Cloud, GKE, Cloud Build, Codemagic, Core Lightning, c-lightning-rest, LNURL, lightning addresses, BOLT11, BOLT card/NFC, on-chain Bitcoin flows, iOS/Android release pipelines, shared model generation.

### Repository

- Local repo inspected: `/Users/sudonym/Documents/Codex/2026-07-06/i-wo/work/sudonym`
- GitHub remote: `git@github.com:sudonym-btc/sudonym.git`
- GitHub repo URL: `https://github.com/sudonym-btc/sudonym`
- Main branch: `main`
- Repo status: private.

### Product Scope

The README describes Sudonym as a "whitelabel-able bitcoin and lightning wallet." Source-backed capabilities include:

- Flutter mobile wallet app for iOS and Android.
- Mock, development, test, staging, and production app entrypoints.
- Multi-currency pot/account-style wallet UX.
- Send and receive flows for Lightning, LNURL, lightning addresses, BOLT11/BOLT12-style receive widgets, and on-chain Bitcoin addresses.
- Transaction list, contacts, bill split, settings, sign-in, splash/home, QR, amount, pay, withdraw, receive, and receiver flows.
- BOLT card/NFC support, including iOS NFC capability declarations and app text saying NFC is needed to read BOLT cards.
- Firebase authentication, push messaging, and app installations.
- Google, Facebook, Twitter, and Apple sign-in packages in the Flutter app.
- Local mock repositories for app development without a live backend.
- Homepage assets and marketing links for TestFlight, Google Play, Telegram, and Twitter.

### Architecture

Major folders:

- `api`: NestJS backend for wallet, transaction, payment, auth, contacts, bill split, Lightning, LNURL, settlement, and notification modules.
- `app`: Flutter mobile client.
- `models`: shared model layer with TypeScript models, Dart model generation, and mock data generation.
- `infrastructure`: Google Cloud Deployment Manager templates, Kubernetes manifests/spec builders, Dockerfiles, minikube setup, Cloud Build helpers, DNS/storage/database/lightning resources, and deploy scripts.
- `homepage`: static landing page and real wallet screenshot assets.

### Flutter App

Mobile app details:

- App name/package: `sudonym`.
- Version in `pubspec.yaml`: `1.0.2+1`.
- Description: "Sudonym bitcoin wallet."
- Dart SDK range in source: `>=2.18.0 <3.0.0`.
- Entry points: `main_mock.dart`, `main_development.dart`, `main_test.dart`, `main_staging.dart`, `main_production.dart`, plus `main.dart`.
- App config domains/API targets:
  - mock: `mock.app`, API `http://10.0.2.2:3000`.
  - development: `local.development`, API `https://api.local.development`.
  - test: `test.sudonym.app`, API `https://api.test.sudonym.app`.
  - staging: `staging.sudonym.app`, API `https://api.staging.sudonym.app`.
  - production: `sudonym.app`, API `https://api.sudonym.app`.

Flutter dependencies and capabilities:

- State/data: `flutter_bloc`, `bloc_test`, `equatable`, `dartz`, `get_it`, `injectable`, `injectable_generator`, `rxdart`, `reactive_forms`, `flutter_form_builder`.
- Networking/cache/logging: `dio`, `dio_cache_interceptor`, `pretty_dio_logger`, `http`, `logger`, `loggy`, `web_socket_channel`.
- Firebase/auth: `firebase_core`, `firebase_auth`, `firebase_auth_mocks`, `firebase_messaging`, `firebase_app_installations`, `firebase_auth_mocks`, `google_sign_in`, `google_sign_in_mocks`, `flutter_facebook_auth`, `twitter_login`, `sign_in_with_apple`.
- Device/UI: `mobile_scanner`, `qr_flutter`, `nfc_manager`, `local_auth`, `image_picker`, `clipboard`, `share_plus`, `url_launcher`, `vibration`, `flutter_local_notifications`, `flutter_ringtone_player`, `sliding_up_panel`, `smooth_page_indicator`, `flutter_slidable`, `font_awesome_flutter`, `auto_animated`, `extended_masked_text`, `money_formatter`, `timeago`, `timeago_flutter`, `navigation_history_observer`, `package_info_plus`, `native_flutter_proxy`, `universal_io`.
- Bitcoin/crypto parsing: `bech32`, `crypto`.
- Testing/dev tools: `flutter_test`, `test`, `http_mock_adapter`, `dependency_validator`, `icons_launcher`, `pubspec_dependency_sorter`, `widgetbook`, `build_runner`.
- Assets: logo, audio, fonts, and currency assets.

Mobile platform details:

- Android application ID: `com.sudonym.sudonym`.
- Android compile SDK in source: `33`; min SDK `21`; target SDK `31`.
- Android uses Google services and Firebase analytics/messaging.
- iOS display name: `Sudonym`.
- iOS URL schemes include `sudonym`, `lnurlw`, `lnurlp`, `lightning`, and `bitcoin`.
- iOS localizations include English, Spanish, and German.
- iOS background modes include fetch and remote notifications.
- iOS declares camera, Face ID, photo library, and NFC usage strings.

### API Backend

Backend framework and packages:

- Package name: `@sudonym/api`.
- NestJS `9`.
- Node/TypeScript backend built with `nest build`.
- Express platform, Swagger/OpenAPI, WebSockets, JWT auth, class validation/transforming, Sentry, and Sequelize/sequelize-typescript with MySQL.
- Runtime dependencies include `@google-cloud/storage`, `firebase-admin`, `bitcoinjs-lib`, `bitcoin-address-validation`, `bolt11`, `lnurl`, `ccxt`, `mysql2`, `sequelize`, `sequelize-typescript`, `ws`, `zeromq`, `cache-manager`, `body-parser`, `uuid`, `unique-username-generator`, and NestJS platform packages.
- Test/build tooling includes Jest, Supertest, Nest testing, Sequelize CLI, ts-jest, ts-loader, ts-node, tsconfig-paths, wait-for-expect, and a Docker MySQL 8 test script.

API modules and services:

- Wallet domain modules: action, auth, bill split, bill split participant, BOLT card, contact, contact address, currency, events, Lightning, LNURL, pot, pot user, receiver, sender, settlement, settlement transaction, transaction, transaction tag, user, and user device.
- Backend services: balance updater, exchange, Firebase Admin, Firebase notifications, Lightning node, roundup, Sentry, transaction bill-split linker, transaction contact linker, and transaction listener.
- Lightning node service talks to Core Lightning/c-lightning-rest endpoints, reads macaroons, lists funds/channels/peers, creates invoices, decodes invoices, estimates route fees, pays invoices, and supports on-chain withdrawal.
- Lightning operations parse BOLT11 invoices and estimate fees.
- LNURL operations handle LNURL strings and lightning addresses, including `.well-known/lnurlp/<username>`, LNURL pay/withdraw callbacks, payer data, comments, and fee estimation.
- Transaction listeners watch invoice/sendpay/on-chain events, create received/spent transactions, and wait for outgoing payment settlement.
- Roundup service creates internal roundup payments to configured roundup pots after outgoing transactions.

### Shared Models

Shared model layer:

- Root package: `@sudonym/models-combined`.
- TypeScript package: `@sudonym/models`.
- Dart package: `sudonym.models`.
- Scripts convert TypeScript models to Dart and generate mocks.
- Tooling includes `ts2dart`, `factory.ts`, `@faker-js/faker`, `madge`, `ts-node`, and TypeScript.
- Model mocks cover wallet contacts, BOLT cards, transactions, pots, users, and other app entities.

### Infrastructure and Cloud

Cloud provider: Google Cloud.

Infrastructure style:

- Uses Google Cloud Deployment Manager Python templates and YAML configs, not Terraform.
- Environments include test, staging, and production configs.
- Root scripts include `init:gcloud`, `deploy:production`, `deploy:staging`, `deploy:test`, `deploy:development`, minikube helpers, lightning initialization, and Cloud SQL proxy helpers.

GCP/Kubernetes components found in source:

- GKE clusters with workload identity.
- Cloud SQL MySQL instance access through Cloud SQL Proxy.
- Google Cloud Storage config/backup buckets.
- Cloud DNS records for API/app/root/web/db/lightning-related subdomains.
- Kubernetes resources for API, DB, homepage, web, Bitcoin node, Core Lightning, c-lightning-rest, and Ride the Lightning.
- Core Lightning/c-lightning-rest services, including multiple local/minikube Lightning nodes.
- Bitcoin node services.
- Nginx static containers for homepage and Flutter web builds.
- API Dockerfile based on Node 18 Alpine, with build/dev/production stages and ZeroMQ libraries.
- Cloud Build deploys through a Helm builder and `infrastructure/deploy.sh`.

Cloud Build:

- Builds custom Helm and Flutter builders when missing.
- Tags the API image with `$SHORT_SHA` and `latest`.
- Builds API Docker image from `infrastructure/api.Dockerfile`, target `production`.
- Pushes `gcr.io/$PROJECT_ID/api:$SHORT_SHA` and `gcr.io/$PROJECT_ID/api:latest`.
- Deploys through a Helm builder using branch/environment name.
- Uses `us-central1-c` cluster/zone substitutions.

### CI/CD and App Stores

GitHub Actions:

- `.github/workflows/tests.yaml` runs on pushes to `production` and pull requests to `production`, `staging`, and `test`.
- App job runs `cd app && flutter test`.
- API job sets up Node, builds `api`, runs tests, and uses a MySQL 8 service.

Codemagic:

- `codemagic.yaml` contains Android and iOS production workflows.
- Android production workflow triggers on `production`, builds app bundle and APK with `lib/main_production.dart`, and uses Google Play publishing configuration for the internal track as draft.
- iOS production workflow triggers on `production`, uses macOS/M1 build machine, builds an IPA with `lib/main_production.dart`, reads App Store/TestFlight build numbers, and submits to TestFlight beta group `beta`.
- Codemagic notifications include email and Slack wiring.
- App Store Connect integration is named `SUDONYM LTD` in source.
- Publishing email in source is `patrick@sudonym.app`.

### Sudonym Verification Notes

- A real wallet visual was found at `homepage/assets/images/screens.jpeg` and copied into the portfolio as `public/assets/sudonym-wallet-real.jpeg`.
- Attempting `flutter pub get` with current Flutter `3.41.7` failed because the app pins older Dart/Flutter-era dependencies, especially `intl ^0.17.0` versus current `flutter_localizations` requiring `intl 0.20.2`. Treat the source as an older archived/demo codebase unless Patrick modernizes dependency constraints.

## Project: Hostr

### One-Line Summary

Hostr is a peer-to-peer short-term rental marketplace built on Nostr, Bitcoin/Lightning, and self-custodial escrow ideas, with a Flutter app, Dart SDK/CLI, custom Nostr models, escrow daemon/contracts, Terraform/GCP infrastructure, Docker local stack, and AI/MCP automation surface.

### Project Skills

- Flutter, Dart, Nostr, NIP modeling, Lightning, Boltz, Rootstock/EVM escrow, Solidity, Hardhat, Dart SDK design, CLI/daemon design, MCP server design, TypeScript, Node.js, Docker Compose, Terraform, Google Cloud, Artifact Registry, GitHub Actions, Retype docs, local TLS/dev stacks, Nostr relay authorization, Blossom uploads.

### Repository

- Local repo: `/Users/sudonym/Desktop/hostr`
- GitHub remote: `git@github.com:sudonym-btc/hostr.git`
- Main branch: `main`
- Public docs referenced in README: https://sudonym-btc.github.io/hostr/
- Project domain surfaced in code/infrastructure: `hostr.network`

### Product Scope

Hostr is described in the README as "Rental accommodation using purely peer-to-peer technologies such as Nostr." The monorepo includes:

- Flutter client app for stores and web.
- Hostr SDK for relay access, models, trades, messaging, and swaps.
- CLI and Dart daemon for local automation and MCP workflows.
- Custom Nostr event model package.
- Escrow daemon, Solidity contracts, and settlement tooling.
- Node/TypeScript MCP server and agent-facing automation.
- Terraform infrastructure, DNS, CI deploy, and hosted VM setup.
- Docker local/dev/hosted stack for relay, Blossom, EVM, observability, and payment services.
- Draft NIPs for accommodation listings, reservation lifecycle, and escrow services.

### Architecture

Major folders:

- `app`: Flutter client app deployed to stores and web.
- `hostr_sdk`: Dart SDK for relays, Hostr models, trades, messaging, swaps, and account allocation.
- `hostr_cli`: Dart command-line interface and daemon for desktop agents/local automation.
- `models`: Dart package for custom Nostr events, kinds, parsing, and validation.
- `escrow`: Dart escrow daemon, JSON-RPC/socket handlers, Solidity contracts, and settlement tooling.
- `escrow/contracts`: Hardhat/Solidity smart contracts, including `MultiEscrow.sol`.
- `ai/mcp-server`: Node/TypeScript MCP server for AI/plugin workflows.
- `relay-authz`: Node gRPC sidecar for Nostr relay authorization policies.
- `infrastructure`: Terraform for GCP, DNS, Artifact Registry, monitoring, compose VM, secrets, and CI identity.
- `docker`: Compose services, Nginx vhost config, TLS, local chain scripts, Blossom config, Boltz config, observability config.
- `dependencies`: Vendored or pinned dependencies including Boltz regtest, NMDK, Google Maps Flutter Web override, and Nostr tooling.

### Frontend and Mobile Technologies

Flutter app:

- Flutter, Dart SDK `^3.10.0`.
- Routing: `auto_route`.
- Dependency injection: `get_it`, `injectable`.
- State: `flutter_bloc`, `hydrated_bloc`, `equatable`, `provider`, `rxdart`.
- Persistence and device features: `flutter_secure_storage`, `path_provider`, `shared_preferences`, `sqlite3`, `workmanager`, `permission_handler`, `flutter_local_notifications`, `app_links`.
- Maps/location: `google_maps_flutter`, `google_maps_flutter_web`, `h3_flutter`.
- UI/media: `carousel_slider`, `sliding_up_panel`, `timelines_plus`, `date_count_down`, `qr_flutter`, `mobile_scanner`, `image_picker`, `image`, `blurhash_dart`, `flutter_svg`, `google_fonts`, `toastification`.
- Crypto/Bitcoin/Nostr/EVM: `ndk`, `web3dart`, `wallet`, `bolt11_decoder`, `coinlib_flutter`, `pointycastle`, `crypto`, `bech32`, `bip39_mnemonic`, `webcrypto`.
- API/codegen: `chopper`, `json_annotation`, `json_serializable`, `build_runner`, `swagger_dart_code_generator`.
- Testing: `flutter_test`, `integration_test`, `flutter_driver`, `mockito`, `mocktail`, `bloc_test`, `shelf`, `shelf_router`, `mocktail_image_network`.
- Launchers/icons: `flutter_launcher_icons`.
- App assets: SVG logo, generated launcher icons, Nostr client assets, Inter variable font.

Flutter app environment entrypoints:

- `main_development.dart`
- `main_staging.dart`
- `main_production.dart`
- `main_mock.dart`

App automation:

- Screenshots: `app/bin/generate_screenshots.sh`
- Widgetbook/storybook-style UI build: `app/bin/generate_widgetbook.sh`
- Icons from single source SVG: `app/bin/generate_icons.sh` plus `dart run flutter_launcher_icons`

Mobile app-store details:

- Android namespace and application ID: `com.sudonym.hostr`.
- Android release signing is read from `android/local.properties`, with CI decoding a base64 keystore secret.
- iOS bundle ID: `com.sudonym.hostr`.
- iOS display name: `Hostr`.
- iOS URL scheme: `nostr`.
- iOS background modes: `processing`, `fetch`.
- iOS BGTaskScheduler identifiers: `com.sudonym.hostr.task.periodic` and `com.sudonym.hostr.task.oneoff`.
- iOS permissions include camera, photo library, and calendar usage descriptions.
- App builds use Flutter stable `3.41.1` in GitHub Actions.
- CI has Google Play upload wiring via `r0adkll/upload-google-play`, but app-store uploads are currently disabled with `ENABLE_APP_STORE_UPLOADS=false`.
- Manual production app builds require explicit `confirm_production_release=true`.

### Hostr Dart Backend/SDK/CLI Packages

`models` package:

- Purpose: Nostr-based accommodation models and validation.
- Direct dependencies include `convert`, `ndk`, `faker`, `crypto`, `logger`, `h3_dart`, `coinlib`.
- Provides custom Nostr model classes for listings, orders, escrow services, reviews, payment methods, messages, profiles, typing indicators, badges, imeta, and related tags.

`hostr_sdk` package:

- Purpose: Dart SDK for Hostr relay/trade/message/swap logic.
- Direct dependencies include `models`, `ndk`, `ascii_qr`, `cbor`, `cryptography`, `elliptic`, `ffi`, `native_toolchain_rust`, `ndk_bip32_keys`, `sembast`, `web_socket_client`, `injectable`, `rxdart`, `logger`, `web3dart`, `equatable`, `coinlib`, `wallet`, `bolt11_decoder`, `http`, `hydrated_bloc`, `chopper`, `bloc`, `h3_dart`, `crypto`, `bip39_mnemonic`, `convert`, `eip712`, `permissionless`, `opentelemetry`, `sqlite3`, `web_socket_channel`, `bip340`, `logging`, `mockito`, `shelf`, and `shelf_router`.
- Dev/codegen dependencies include `build_runner`, `chopper_generator`, `injectable_generator`, `json_serializable`, `swagger_dart_code_generator`, `web3dart_builders`, and `test`.
- Uses local override for `ndk` from `sudonym-btc/ndk` branch `feat/hostr-fixes`.
- Uses a custom `web3dart_builders` fork from `PatrickGeyer/web3dart_builders` branch `feat/upgrade-deps`.

`hostr_cli` package:

- Purpose: Hostr command-line interface and daemon for desktop agents/local automation.
- Executables: `hostr`, `hostr-cli`, `hostr-daemon`.
- Direct dependencies include `args`, `crypto`, `hostr_sdk`, `logger`, `logging`, `mime`, `models`, `ndk`, `path`, `qr`, `sqlite3`.
- Tests use `test`; linting uses `lints`.

`escrow` package:

- Purpose: Dart service responding to Nostr messages and escrow events.
- Executable: `escrow_daemon`.
- Direct dependencies include `args`, `get_it`, `injectable`, `http`, `json_rpc_2`, `stream_channel`, `models`, `hostr_sdk`, `ndk`, `h3_dart`, `artisanal`, `dart_console`, `interact_cli`, `web3dart`, `wallet`, and `crypto`.

### Hostr Smart Contracts

`escrow/contracts`:

- Hardhat project.
- Solidity contract: `contracts/MultiEscrow.sol`.
- Dependencies: `@openzeppelin/contracts`.
- Dev dependencies: `@nomicfoundation/hardhat-toolbox`, `hardhat`, `typescript`.
- Contains Hardhat tasks such as deploy/fund and an Ignition module.
- Local chain support includes Foundry/Anvil, WETH, MockTBTC, MockUSDT, Multicall3, Uniswap V3 bytecodes, and Arbitrum-regtest initialization scripts.

### Hostr Node/AI/Relay Packages

`ai/mcp-server`:

- Package name: `@hostr/mcp-server`.
- Node/TypeScript MCP server.
- Scripts: `build`, `dev`, `dev:local`, `start`, `test:unit`, `test:ai:e2e`, `test:ai:e2e:supervised`, `test:ai:e2e:smoke`, `typecheck`.
- Runtime dependencies: `@modelcontextprotocol/sdk`, `express`, `jose`, `zod`.
- Dev dependencies: `typescript`, `tsx`, `@types/node`, `@types/express`.

`relay-authz`:

- Package name: `@hostr/relay-authz`.
- Node gRPC sidecar for Nostr relay authorization.
- Scripts: `start`, `test`.
- Dependencies: `@grpc/grpc-js`, `@grpc/proto-loader`.
- Has protobuf definition `relay-authz/proto/nauthz.proto`.
- Dockerfile builds the sidecar image.

### Hostr Infrastructure and Cloud

Cloud provider: Google Cloud.

Terraform provisions and manages:

- GCP project resources.
- APIs: Cloud Logging, Cloud Trace, Cloud Monitoring, API Keys, Google Maps services, DNS, Secret Manager, Artifact Registry.
- Google Maps API keys and map IDs for web, Android, and iOS.
- Cloud DNS managed zone records for `relay`, `blossom`, root app domain, `paymaster`, `app`, and `ai`.
- Static regional IP address.
- VPC, firewall rules for compose VM public ports and health checks.
- Single Docker Compose VM and managed instance group for hosted deployment.
- Persistent disks for relay data, Blossom data, and MCP daemon/OAuth state.
- Secret Manager entries for runtime secrets such as escrow key, Blossom dashboard password, and MCP JWT secret.
- A separate AA paymaster signer key stored for bootstrap/admin workflows and intentionally not fetched onto the runtime VM by default.
- Artifact Registry Docker repository with cleanup policies.
- Uptime checks for root, app, relay, and MCP health.
- Email notification channels and alert policies.
- GitHub Actions Workload Identity Federation to GCP using GitHub OIDC and a CI deploy service account.
- Relay disk snapshot schedule with retention.

Hosted compose services:

- `nginx-proxy`
- `acme-companion` for LetsEncrypt certificates.
- `relay`: Nostr relay image built from pinned `nostr-rs-relay`.
- `relay-authz`: relay admission/authorization sidecar.
- `blossom`: Blossom server for blob storage.
- `app`: Flutter web app image.
- `mcp`: Hostr MCP server.
- `otelcol`: OpenTelemetry Collector.
- `escrow`: Dart escrow daemon.

Local/test compose services include:

- `hostrbitcoind`
- LND
- LNBits
- Alby Hub
- Signet
- Boltz regtest stack
- Elements/Liquid support from Boltz regtest
- Foundry/Anvil RSK and Arbitrum-style local chains
- Uniswap V3 deployment scripts
- Cashu mint and Redis
- Account-abstraction/paymaster stack using Pimlico Alto and mock verifying paymaster
- Local TLS generation and Nginx proxy.

### Hostr CI/CD

GitHub Actions workflows:

- `ci.yaml`: path-filtered checks for SDK, app, escrow, MCP, plugins, and Docker root changes.
- `app_build.yaml`: manual and release-coupled Android/iOS builds for staging and production variants.
- `docker_build.yaml`: builds and pushes escrow, app, and MCP Docker images to Artifact Registry.
- `infra_deploy.yaml`: Terraform deploy of infrastructure plus compose managed instance group replacement.
- `compose_redeploy.yaml`: pulls latest images and restarts hosted compose services on the current compose VM/MIG instance.
- `relay_images_build.yaml`: manual-only relay and relay-authz image builds.
- `cloud_logs_read.yaml`: reads MCP/OAuth logs from Cloud Logging or compose VM.
- `retype.yaml`: publishes Retype docs to a `retype` branch.
- `protect_branch.yaml`: attempts branch protection setup.

CI details:

- SDK tests run Dart `3.10.8`, install Docker Compose, start the Docker stack in the background, run unit tests, wait for the stack, add local host aliases, trust a dev CA, then run Docker integration tests.
- Flutter app tests run Flutter `3.41.1`, `flutter test --coverage`, and compile-check Widgetbook web.
- Docker images are tagged with short SHA and `latest`.
- Terraform deploy uses GCS backend and `var/shared.tfvars` plus environment-specific tfvars.
- Hosted redeploy uses `gcloud compute ssh` to run `/usr/local/bin/hostr-deploy`.

### Hostr Codex Plugin

Description:

- `sudonym-btc/hostr-codex` is the Codex marketplace distribution repo for Hostr MCP plugins. It packages Hostr plugin variants for default, development, and staging environments and includes skills that teach Codex how to use Hostr MCP tools for lodging search, listing management, reservations, Nostr signer/session workflows, image uploads, messaging, escrow compatibility, and Lightning-to-smart-contract escrow payment flows.

Project skills:

- Codex plugin packaging, MCP tool UX, skill authoring, Nostr workflow guidance, dry-run/write-safety design, AI-assisted marketplace actions, structured listing cards, plugin environment variants, Hostr booking/payment workflow documentation.

## Project: NMDK / Nostr Marketplace Development Kit

### One-Line Summary

NMDK is a modular development kit for Nostr marketplace work. It pins the marketplace protocol/runtime, browser demo client, EVM escrow driver, Cashu escrow driver, location provider packages, Solidity contract artifacts, protocol drafts, local Docker integration stacks, and docs site into one reproducible development snapshot.

### Project Skills

- Nostr marketplace protocol design, TypeScript package architecture, payment-driver interfaces, Cashu escrow, EVM escrow, ERC-4337 account abstraction, Boltz swap orchestration, Rootstock/Arbitrum-style local chains, Solidity ABI/artifact packaging, H3 geospatial indexing, Docker Compose regtest stacks, Bitcoin Core regtest, LND, LNbits, Alby Hub, nostr-rs-relay, Blossom, local TLS, Next.js docs, Fumadocs, TypeDoc, GitHub Pages, deterministic demo capture.

### Repositories

Source-backed repos inspected:

- `sudonym-btc/nmdk`
- `sudonym-btc/marketplace-app-ts`
- `sudonym-btc/marketplace-evm-ts`
- `sudonym-btc/marketplace-cashu-ts`
- `sudonym-btc/marketplace-evm-contracts`
- `sudonym-btc/marketplace-evm-stack`
- `sudonym-btc/marketplace-cashu-stack`
- `sudonym-btc/marketplace-driver-interface-ts`
- `sudonym-btc/marketplace-location-interface-ts`
- `sudonym-btc/marketplace-location-h3-ts`

### Product Scope

NMDK is an aggregate repository. Its README describes it as the place that pins the marketplace protocol, client runtime, payment drivers, demo clients, and local development stacks used by Nostr marketplace work. The implementation repos remain nested submodules so each package can keep its own release cadence while NMDK provides one reproducible development snapshot.

Core boundary:

- Nostr event semantics live in `nostr-tools/marketplace`.
- Nostr application UX lives in `marketplace-app-ts`.
- Payment-specific behavior lives in payment policy packages such as `marketplace-cashu-ts` and `marketplace-evm-ts`.
- Local integration dependencies live in stack repos.
- Payment packages should not create their own Nostr pools for marketplace order state; they implement a structural marketplace policy interface and receive resolved payment intents from the Nostr runtime.

### NMDK Local Stack

Description:

- `sudonym-btc/nmdk` launches a standalone local marketplace development surface. It starts a Nostr relay, Cashu sat/USD mints, Blossom upload, Arbitrum-style and Rootstock-style RPCs, Boltz API, marketplace LND, LNbits, Alby Hub, EVM account-abstraction bundler/paymaster services, local HTTPS proxy, deterministic seed data, and demo client configuration.
- `npm run demo:up` launches the full stack plus demo client.
- `npm run demo:capture:fresh` resets disposable stack data, starts EVM/Cashu arbiters, captures marketplace flows, and writes screenshots plus a WebM recording under `artifacts/marketplace-demo/<run-id>/`.

Project skills:

- Docker Compose orchestration, Bitcoin/Lightning regtest, local TLS/CA setup, reproducible demo capture, deterministic seed data, Nostr relay setup, Cashu mint setup, Boltz integration, LND channel/liquidity initialization, account-abstraction service orchestration, local developer experience design.

### Marketplace App

Description:

- `sudonym-btc/marketplace-app-ts` is a browser demo client for a Nostr marketplace. It includes NIP-46 bunker/Nostr Connect login, marketplace seed recovery through self-encrypted Nostr seed events, EVM marketplace-driver integration, startup payment-method publishing, startup EVM driver discovery/sweep hooks, classified listings, listing detail checkout, inbox, orders, listing editor, and auction/bid pages.
- The app defaults to NMDK's local relay and can run inside the NMDK dependency tree or in app-only development mode when local services are already running.

Project skills:

- React, Vite, TypeScript, TanStack Router, Radix UI, shadcn-style UI, Tailwind CSS, Nostr Tools, NIP-46/Nostr Connect, Nostr encrypted seed recovery, qrcode rendering, marketplace checkout UX, inbox/order/listing flows, EVM/Cashu payment driver integration, deterministic local-stack configuration.

### Marketplace EVM Driver

Description:

- `sudonym-btc/marketplace-evm-ts` is a Nostr-agnostic EVM payment engine for marketplaces. It owns EVM escrow validation, escrow lifecycle calls, Boltz-backed swap orchestration, operation recovery, and account-abstraction integration.
- It validates escrow funding proofs from transaction receipts and contract logs; builds escrow fund/release/claim/arbitrate/withdraw calls; executes calls through ERC-4337 account abstraction; coordinates Boltz swap-in/swap-out lifecycles; persists and resumes operations through caller-provided storage; derives per-trade account-abstraction owner accounts from a marketplace seed; and reports deterministic usage watermarks for parent-index recovery.

Project skills:

- TypeScript library design, EVM receipt/log validation, viem, permissionless, ERC-4337, SimpleAccount-style account abstraction, Pimlico-compatible paymaster integration, Boltz REST/OpenAPI, swap lifecycle recovery, deterministic account derivation, storage interfaces, integration testing against local stacks.

### Marketplace Cashu Driver

Description:

- `sudonym-btc/marketplace-cashu-ts` is a Nostr-agnostic Cashu payment policy package for marketplace escrow flows. It creates Cashu BOLT11 mint quotes, waits for invoice settlement, mints P2PK-locked escrow proofs, validates payment proofs, and recovers published payment state from proof data without requiring device-local Cashu state.
- The driver exposes proof-local payment terms so the Nostr marketplace layer can compare verified facts against signed order or auction data.

Project skills:

- Cashu, Cashu Nuts, P2PK-locked proofs, BOLT11 mint quotes, proof-state validation, payment recovery, Nostr-agnostic driver interfaces, TypeScript policy design, storage/recovery abstractions, local Cashu mint integration.

### Marketplace EVM Contracts

Description:

- `sudonym-btc/marketplace-evm-contracts` is the source of truth for `MultiEscrow` Solidity source, Hardhat artifact JSON, ABI JSON, generated TypeScript exports, and runtime bytecode hash registry. Consumers import ABI/registry data from this package rather than maintaining hand-written ABI copies.

Project skills:

- Solidity, escrow contract artifacts, ABI packaging, runtime bytecode hashes, TypeScript export generation, contract registry design, package-owned generated artifacts, Hardhat/solc workflows.

### Marketplace EVM Stack

Description:

- `sudonym-btc/marketplace-evm-stack` is a standalone Docker stack for testing marketplace EVM drivers against Boltz and EVM environments. It provides Boltz regtest services, Rootstock-style and Arbitrum-style Anvil chains, Boltz EVM swap contracts, mock tBTC/USDT, Uniswap V3 routing contracts, deployed `MultiEscrow`, local ERC-4337 services, and generated JSON config consumed by TypeScript and Dart tests.

Project skills:

- Docker Compose, Anvil, Rootstock-style local chains, Arbitrum-style local chains, Boltz EVM swaps, Uniswap V3 local routing, mock ERC20 assets, ERC-4337 bundler/paymaster services, deterministic contract deployment, generated test config, multi-stack local development.

### Marketplace Cashu Stack

Description:

- `sudonym-btc/marketplace-cashu-stack` is a Docker stack for testing Cashu marketplace escrow and auction drivers against local Cashu mints and a Nostr relay. It joins the EVM/Boltz stack's Bitcoin regtest network so Cashu, Boltz, and marketplace LND services share one Lightning liquidity domain.
- It provides Cashu Nutshell mints, shared Bitcoin Core regtest access, LND mint/buyer nodes, Cashu BOLT11 backends, Redis-backed cached responses, nostr-rs-relay, and generated JSON config for TypeScript and Dart tests.

Project skills:

- Cashu Nutshell, LND, Bitcoin Core regtest, Lightning channel setup, BOLT11 mint quote settlement, Redis-backed mints, nostr-rs-relay, generated stack config, local integration-test infrastructure, split internal/public service URL design.

### Marketplace Driver Interface

Description:

- `sudonym-btc/marketplace-driver-interface-ts` defines shared TypeScript contracts for marketplace payment drivers. It contains driver-facing data shapes and interfaces only; Nostr relay access and event parsing belong in marketplace runtime code, while concrete chain/mint/swap logic belongs in driver packages.
- It supports encrypted payment-proof params, optional `decryptParams` hooks, and validated payment terms such as settlement ID, payment amount, funded amount, security bond, escrow fee, unlock time, and timeout claimant.

Project skills:

- TypeScript interface design, package-boundary design, encrypted proof-parameter contracts, NIP-44-style payload handling at adapter boundaries, payment validation API design, strongly typed marketplace abstractions.

### Marketplace Location Packages

Description:

- `sudonym-btc/marketplace-location-interface-ts` defines the provider shape for address lookup, area lookup, and marketplace geospatial tag generation without depending on a concrete geocoder or H3 implementation.
- `sudonym-btc/marketplace-location-h3-ts` implements an H3-backed marketplace location provider. It accepts upstream resolvers for address-to-coordinates and area-to-polygon, owns `h3-js`, and returns marketplace-ready `["g", cell]` tags.

Project skills:

- H3 geospatial indexing, GeoJSON polygon/multipolygon handling, address/area resolver interfaces, marketplace geospatial tags, TypeScript provider interfaces, decoupling protocol packages from concrete geospatial dependencies.

### Documentation and Developer Experience

Description:

- NMDK uses a GitHub Pages workflow with Node `24`, recursive submodules, Next.js/Fumadocs static docs builds, and a published docs site.
- Package docs use TypeDoc with `typedoc-plugin-markdown` to generate reference docs under package-owned `docs/reference` folders.
- The scanned marketplace packages expose docs scripts such as `docs`, `docs:api`, `check`, `build`, integration tests, and generated API references.

Project skills:

- Technical writing, package-owned docs, TypeDoc, typedoc-plugin-markdown, Next.js, Fumadocs, MDX, GitHub Pages, submodule-aware CI, reproducible docs generation, API-reference maintenance, developer onboarding docs.

## Project: Nestr

### One-Line Summary

Nestr is a virtual office environment for nostr protocol chatrooms, built with React, TypeScript, Three.js, signer-based auth, encrypted Nostr Connect session restoration, deterministic 3D maps, live group chat, presence, and WebRTC proximity-call modeling.

### Project Skills

- React, TypeScript, Vite, Three.js, GLTFLoader, Tailwind CSS, Radix UI, shadcn-style components, Nostr Tools, NIP-29, NIP-42, NIP-46, NIP-65, NIP-66, NIP-17, NIP-04, IndexedDB/WebCrypto session storage, WebRTC simulation, Playwright, Vitest.

### Repository

- Local repo: `/Users/sudonym/Desktop/nestr`
- GitHub remote: `git@github.com:sudonym-btc/nestr.git`
- Main branch: `main`

### Important Source Note

The README says "Phaser-powered walkable office canvas," but the current source imports and uses Three.js and `GLTFLoader` in `src/game/OfficeRenderer.tsx`. Prefer the code-backed description: React + TypeScript + Three.js.

### Product Scope

Nestr prototype features:

- NIP-29 spatial-office model for Nostr users.
- Relay-based groups become deterministic office rooms.
- NIP-29 group metadata drives room state.
- Global sidebar chat stays available.
- Ephemeral office presence renders avatars in a shared map.
- Mock relay simulates group metadata, members, roles, and chat.
- Live NIP-29 launch mode from `?c=<group-id>&relay=<relay-host>`.
- NIP-07 browser signer login.
- NIP-46/Nostr Connect login paths.
- NIP-42 relay authentication after signer connect.
- Origin-bound encrypted IndexedDB storage for restored Nostr Connect sessions.
- `kind:25029` ephemeral avatar movement events.
- Tweened remote avatar movement.
- Deterministic avatars derived from npub/pubkey.
- P2P WebRTC mesh pressure estimator for proximity calls.
- Mock WebRTC call grid with generated remote peer streams and fullscreen mode.
- NIP-66 relay discovery code for relays advertising NIP-29 support.
- NIP-65 profile relay list caching.
- Blossom upload helper.
- NIP-17 and NIP-04 direct message handling.
- NIP-51 simple group parsing.

### Frontend Technologies

Core:

- Vite.
- React `19`.
- TypeScript `~6.0`.
- Three.js `0.184`.
- `GLTFLoader` for office asset models.
- Tailwind CSS `4`.
- `@tailwindcss/vite`.
- shadcn-style UI components.
- Radix UI.
- `lucide-react`.
- `class-variance-authority`, `clsx`, `tailwind-merge`, `tw-animate-css`.
- `@fontsource-variable/geist`.

Nostr and crypto:

- `nostr-tools`.
- `@noble/hashes`.
- NIP-07, NIP-42, NIP-46, NIP-29, NIP-51, NIP-65, NIP-66, NIP-17, NIP-04 code paths.
- WebCrypto/IndexedDB for encrypted local Nostr Connect session storage.

Media/visual:

- `qrcode`.
- 3D GLB office asset pack.
- WebRTC media simulation and fake camera/microphone e2e permissions.

### Nestr Direct Dependencies

Runtime dependencies:

- `@fontsource-variable/geist`
- `@noble/hashes`
- `@tailwindcss/vite`
- `class-variance-authority`
- `clsx`
- `lucide-react`
- `nostr-tools`
- `qrcode`
- `radix-ui`
- `react`
- `react-dom`
- `shadcn`
- `tailwind-merge`
- `tailwindcss`
- `three`
- `tw-animate-css`

Dev dependencies:

- `@eslint/js`
- `@playwright/test`
- `@testing-library/jest-dom`
- `@testing-library/react`
- `@types/node`
- `@types/qrcode`
- `@types/react`
- `@types/react-dom`
- `@types/three`
- `@vitejs/plugin-react`
- `eslint`
- `eslint-plugin-react-hooks`
- `eslint-plugin-react-refresh`
- `globals`
- `jsdom`
- `typescript`
- `typescript-eslint`
- `vite`
- `vitest`

### Nestr Scripts and CI

Package scripts:

- `npm run dev`: Vite dev server.
- `npm run build`: TypeScript build plus Vite build.
- `npm run lint`: ESLint.
- `npm test`: Vitest.
- `npm run test:watch`: Vitest watch.
- `npm run e2e`: Playwright.

GitHub Actions:

- `.github/workflows/deploy.yml` deploys to GitHub Pages on push to `main` or manual dispatch.
- Uses Node.js `22`, `npm ci`, `npm run build`, uploads `dist`, and deploys with `actions/deploy-pages`.

Testing:

- Vitest for logic tests in `src/**/*.test.ts` and `src/**/*.test.tsx`.
- Playwright e2e smoke tests in `tests/e2e`.
- Playwright config launches Vite on `127.0.0.1:5173`, grants camera/microphone permissions, and uses fake media devices.

## Project: Evolut

### One-Line Summary

Evolut was a SaaS platform for automating LinkedIn marketing workflows, with an Angular dashboard, NestJS/Node API, headless Puppeteer bot workers, MySQL/Sequelize data layer, Firebase auth, Google Cloud Pub/Sub task queues, GCP Storage for assets/config/releases, Cloud Build and GKE/App Engine deployments, Stripe billing, SendGrid email, Zapier integration, and a white-label Electron desktop app generator.

### Project Skills

- Angular, Angular Material, NestJS, Node.js, TypeScript, Puppeteer, MySQL, Sequelize, Firebase, Google Cloud Pub/Sub, Google Cloud Storage, Cloud Build, GKE, App Engine Flexible, Docker, Kubernetes, Stripe, SendGrid, Zapier Platform, Electron, white-label builds, headless browser automation, proxy/VPN-aware automation workers.

### Source Repositories

Private GitHub repos found under `pats2sats`:

- `linkedin_frontend`: Angular dashboard and landing/site frontend. Package name `lnqd.in`.
- `linkedin_api`: NestJS API and core backend. Package name `evolut-api`.
- `linkedin_bot`: Puppeteer/NestJS automation worker. Package name `linkedin_bot`.
- `linkedin_zapier`: Zapier Platform integration. Package name `evolut`.
- `linkedin_downloadable`: Electron app build/publishing package. Package name `downloadable`.
- `linkedin_default`: default App Engine dispatch/development compose/generated API client support.
- `linkedin_api_old`: older Express/TypeScript API implementation. Package name `linkedin-api`.
- `linkedin_cloud_functions`: Google Cloud Functions image resizing utility.

### Product Scope

Source-backed Evolut product capabilities:

- Company/client management.
- User/accounts management for LinkedIn automation.
- Search workflows and search history.
- LinkedIn invitation/message templates.
- Bulk message workflows.
- Journey/status flows.
- Task and task-attempt tracking.
- Conversation and inbox views.
- Post automation, post likes, comments, shares, reshares, post bundles.
- Endorsement and endorsement-bundle workflows.
- Web tracking and web visit triggers.
- Webhooks and webhook attempts.
- Tags and user tags.
- Stripe-backed billing.
- SendGrid-backed email templates.
- Zapier resources, triggers, and actions.
- White-label/company share flows.
- White-label desktop downloadable builds.

### Evolut Frontend

Repo: `linkedin_frontend`.

Framework and build:

- Angular `8.2`.
- TypeScript `3.5`.
- Angular CLI `8.3`.
- Angular Material/CDK `8.2`.
- Angular Flex Layout.
- Fuse Angular admin-template-style structure.
- SCSS.
- Nginx production container.
- Docker multi-stage build from Node `12.13.0` to Nginx `1.13`.
- App Engine flexible `app.yaml` for default service.
- GKE/Kubernetes manifests for frontend deployment, service, ingress, cert-manager, and certificates.

Frontend packages:

- `@angular/animations`
- `@angular/cdk`
- `@angular/common`
- `@angular/compiler`
- `@angular/core`
- `@angular/flex-layout`
- `@angular/forms`
- `@angular/http`
- `@angular/material`
- `@angular/material-moment-adapter`
- `@angular/platform-browser`
- `@angular/platform-browser-dynamic`
- `@angular/router`
- `@ngrx/effects`
- `@ngrx/router-store`
- `@ngrx/store`
- `@ngrx/store-devtools`
- `@ngx-translate/core`
- `@swimlane/ngx-charts`
- `@swimlane/ngx-datatable`
- `@swimlane/ngx-dnd`
- `angular-calendar`
- `angular-in-memory-web-api`
- `angular-mentions`
- `angular-text-input-highlight`
- `angularfire2`
- `chart.js`
- `classlist.js`
- `core-js`
- `d3`
- `deepmerge`
- `firebase`
- `hammerjs`
- `linkedin_shared`
- `lodash`
- `moment`
- `ng2-charts`
- `ngrx-store-freeze`
- `ngx-color-picker`
- `ngx-cookie-service`
- `ngx-moment`
- `ngx-textarea-autosize`
- `parse-domain`
- `perfect-scrollbar`
- `prismjs`
- `rxjs`
- `rxjs-compat`
- `seed-random`
- `socketio-wildcard`
- `url-parse`
- `web-animations-js`
- `zone.js`

Shared Bit components:

- `@bit/evolut.shared.shared.event`
- `@bit/evolut.shared.shared.interface`
- `@bit/evolut.shared.shared.model`
- `@bit/evolut.shared.shared.progress`
- `@bit/evolut.shared.shared.sample`
- `@bit/evolut.shared.shared.utility`
- `@bit/evolut.shared.shared.validator`

Frontend dev/test packages:

- Storybook for Angular `5.3`.
- Karma/Jasmine test tooling.
- Protractor e2e tooling.
- TSLint/Codelyzer.
- Webpack bundle analyzer.
- `bit-bin`.

Frontend modules in source:

- Account, authentication, billing, bulk message, client, company, endorsement, entity, journey, knowledge base, lookup, post, search, tag, task.
- Core services for auth, API entities, audio, graph, inbox, list handling, sockets, toast, and guards/interceptors.
- Shared preview cards/chips for accounts, users, searches, posts, companies, endorsement bundles, and bulk messages.

Frontend environment/deploy facts:

- Production API base URL: `https://api.evolut.net`.
- Staging API base URL: `https://api.beta.evolut.net`.
- Firebase project ID in frontend environment: `linkedin-207520`.
- Domains surfaced: `www.evolut.net`, `portal.evolut.net`, `api.evolut.net`.
- Uses Firebase/AngularFire auth in frontend.
- Uses Stripe publishable configuration in frontend environment. Do not expose or repeat keys beyond saying Stripe was integrated.
- Embeds Chatlio widget in `src/index.html`.
- Contains `track.js` and `common-web` assets for site/customer web tracking.
- Contains knowledge-base content for onboarding users through companies, accounts, searches, billing, and message setup.

Frontend CI/deploy:

- `cloudbuild.yaml` builds Docker image `gcr.io/$PROJECT_ID/frontend:$SHORT_SHA` and `latest`.
- Pushes image to GCR.
- Updates Kubernetes deployment `frontend-deploy` in cluster `frontend`, zone `us-central1-a`.
- Cloud Build machine type: `N1_HIGHCPU_8`.

Frontend caveat:

- `karma.conf.js` contains unresolved merge-conflict markers in the inspected private source. Do not claim the frontend test config is currently clean without confirmation.

### Evolut API

Repo: `linkedin_api`.

Framework and build:

- Package name: `evolut-api`.
- Node/TypeScript backend.
- NestJS `6.8`.
- TypeScript `3.6`.
- Express platform.
- Swagger/OpenAPI via `@nestjs/swagger` and `swagger-ui-express`.
- Socket.IO/WebSockets via Nest platform-socket.io and `socket.io`.
- Sequelize/sequelize-typescript with MySQL.
- Docker image based on Node `12.4.0`.
- App Engine flexible custom runtime service `api`.
- GKE deployment image updated by Cloud Build.
- Bit components used for shared models, progress, bot errors, and environment loading.

API runtime packages:

- `@nestjs/common`
- `@nestjs/core`
- `@nestjs/microservices`
- `@nestjs/platform-express`
- `@nestjs/platform-socket.io`
- `@nestjs/swagger`
- `@nestjs/websockets`
- `express`
- `sequelize`
- `sequelize-typescript`
- `mysql2`
- `socket.io`
- `rxjs`
- `reflect-metadata`
- `class-transformer`
- `class-validator`
- `firebase-admin`
- `jsonwebtoken`
- `stripe`
- `@sendgrid/mail`
- `email-templates`
- `handlebars`
- `multer`
- `@google-cloud/dns`
- `@google-cloud/language`
- `@google-cloud/logging-bunyan`
- `@google-cloud/pubsub`
- `cache-manager`
- `bunyan`
- `bunyan-format`
- `continuation-local-storage`
- `cls-hooked`
- `countries-and-timezones`
- `moment`
- `moment-timezone`
- `request-promise`
- `promise-retry`
- `microjob`
- `multiprocessing`
- `node-sass`
- `easyimage`
- `get-audio-duration`
- `uuid`
- `seedrandom`
- `yaml`
- `downloadable` from local `../downloadable`

API test/build packages:

- Mocha.
- Chai.
- Chai-as-promised.
- Chai-subset.
- Supertest.
- Jest/ts-jest types/tooling.
- `@nestjs/testing`.
- Nodemon.
- Prettier.
- TSLint.
- Webpack/ts-loader/webpack-node-externals.

API scripts:

- `npm run build`: TypeScript compile.
- `npm run build:sass`: builds email/view CSS from Sass.
- `npm run start`: loads environment then runs `src/main.ts` with ts-node.
- `npm run start:dev`: nodemon dev server.
- `npm run start:prod`: runs `src/main.ts` with ts-node after production prestart.
- `npm run test`: Mocha tests under `src/test/**/*.spec.ts`.
- `npm run test:prod`: production-style test run.
- `npm run test:load`: load tests.
- `npm run test:build`: build-focused worker tests.
- `npm run test:kubernetes`: Kubernetes worker tests.

API modules in source:

- Account, account-user, audio, bot, bot-connect, bulk-message, bulk-message-account, bulk-message-search, bulk-message-tag, client, company, company-share, conversation, conversation-item, endorsement, endorsement-bundle, episode, image, inbox, inbox-view, journey, journey-status, message, message-template, post, post-bundle, post-comment, post-like, post-reshare, post-share, proxy, search, search-history, search-user, search-user-template, stripe, system-email, tag, task, task-attempt, task-attempt-image, task-attempt-verification, user, user-history, user-skill, user-tag, user-unsubscribe, web-track, webhook, webhook-attempt, webhook-trigger.

API shared systems:

- Auth model.
- Cache.
- Database provider using Sequelize CLS namespace.
- HTTP exception filter.
- Logging interceptor.
- Pub/Sub providers.
- WebSocket layer.
- Environment loader pulling `.env` and Firebase service config from a GCS bucket named `configuration-linkedin`.
- Kubernetes helper service that applies cert-manager, certificates, service, deployment, and ingress manifests.

API data layer:

- MySQL database.
- Sequelize + sequelize-typescript models.
- Automatic schema creation/sync in provider.
- Encrypted ID fields and encrypted model fields via hooks.
- Test environment can create unique `linkedin_test_*` schemas for parallel-ish tests.
- Tables/models include accounts, users, companies, searches, search users, messages, conversations, journeys, tasks, task attempts, webhooks, web tracking, posts, endorsements, bulk messages, images, audio, and system emails.

API Pub/Sub:

- Uses Google Cloud Pub/Sub.
- Topics include:
  - `task-<env>`
  - `taskAccepted-<env>`
  - `task-progress-<env>`
  - `webhook-<env>`
- Subscriptions are created automatically when needed.

API CI/deploy:

- `cloudbuild.yaml` decrypts application credentials using Google Cloud KMS keyring/key `app`.
- Builds Docker image `gcr.io/$PROJECT_ID/api:$SHORT_SHA` and `latest`.
- Pushes both tags.
- Pulls DB image `gcr.io/$PROJECT_ID/db:latest`.
- Starts app and DB with Docker Compose in Cloud Build.
- Runs `npm run test:prod` inside API container.
- Updates Kubernetes deployment `api-deploy` in cluster `api`, zone `us-central1-a`.
- Cloud Build machine type: `N1_HIGHCPU_8`.

### Evolut Bot Worker

Repo: `linkedin_bot`.

Purpose:

- Listens to `task-${NODE_ENV}` Pub/Sub topic for outstanding tasks.
- Uses Puppeteer/headless Chromium to execute LinkedIn automation workflows.
- Uses Xvfb in Docker for browser automation.
- Connects back to the API/core server by Socket.IO client.
- Uses proxy credentials assigned to accounts.
- Records request/download/page metrics into task attempts.

Framework and build:

- Node/TypeScript.
- NestJS `6.8`.
- TypeScript `3.6`.
- Puppeteer `2.0`.
- Docker base: `launcher.gcr.io/google/nodejs`.
- Installs Chromium/X11/Xvfb dependencies for Puppeteer.
- Exposes ports `3002`, `8080`, `80`, `443`.
- App Engine flexible custom runtime service `bot`.

Bot packages:

- `@nestjs/common`
- `@nestjs/core`
- `puppeteer`
- `xvfb`
- `express`
- `socket.io-client`
- `request`
- `request-promise-native`
- `async-retry`
- `promise-retry`
- `@google-cloud/pubsub`
- `@google-cloud/storage`
- `@google-cloud/logging-bunyan`
- `bunyan`
- `bunyan-format`
- `jsonwebtoken`
- `cookie`
- `cookie-parser`
- `dotenv`
- `fs-extra`
- `reflect-metadata`
- `rxjs`
- `rxjs-compat`
- `string-similarity`
- `uuid`
- `url-parse`
- `typescript-ioc`
- `inversify-inject-decorators`
- `ttypescript`
- `typescript-transform-paths`
- Shared Bit packages for Evolut interfaces and models.

Bot modules/workers:

- Account worker.
- Messaging worker.
- Connection worker.
- Search worker with parsers/preparers for blended, sales, cluster, and group searches.
- User worker.
- Post worker.
- Request services for core API and LinkedIn HTTP requests.
- Page manager using Puppeteer headers/pages.
- Socket service.
- Logger service.

Bot CI/deploy:

- `cloudbuild.yaml` decrypts application credentials with GCP KMS.
- Builds Docker image `gcr.io/linkedin-207520/bot:$SHORT_SHA` and latest.
- Pushes images.
- Updates Kubernetes deployment `bot-deploy` in cluster `bot`, zone `us-central1-a`.

### Evolut Desktop / Downloadable App

Repo: `linkedin_downloadable`.

Purpose:

- Builds a white-label Electron downloadable desktop app per company.
- Reads company data through shared API client.
- Writes company-specific config with `portal` and `publishPlatform`.
- Resizes company icon with Sharp.
- Builds Electron app via `electron-builder`.
- Publishes generic auto-update metadata/files to Google Cloud Storage bucket `linkedin-release`.
- Uses `electron-updater`.

Packages:

- Electron `4.0.0`.
- `electron-builder`.
- `electron-updater`.
- `electron-publisher-gcs`.
- `electron-rebuild`.
- `electron-notification-state`.
- `node-notifier`.
- `sharp`.
- `nodegit`.
- `semver`.
- `yaml`.
- `@google-cloud/storage`.
- `@nestjs/common`, `@nestjs/core`.
- Shared Bit API client/model packages.
- Depends on `linkedin_bot`.

Scripts:

- `npm run build-tsc`
- `npm run start`
- `npm run build`
- `npm run deploy`

Notes from README:

- Requires an Apple developer account to sign the app.
- Requires GitHub token in electron-builder config.
- Pinned to Electron `4.0.0` because it uses Node 10, avoiding node-gyp rebuild issues from newer Node versions.

### Evolut Zapier Integration

Repo: `linkedin_zapier`.

Purpose:

- Zapier Platform integration exposing Evolut resources, triggers, and actions.

Packages:

- `zapier-platform-core` `7.5.0`.
- TypeScript.
- Mocha.
- Should.
- Shared Bit packages for Evolut event/model/sample.

Zapier scripts:

- `npm run zapier-build`: TypeScript compile.
- `npm run zapier-dev`: watch compile.
- `npm run zapier-push`: build then `zapier push`.
- `npm test`: Mocha tests.

Zapier resources:

- Account.
- Company.
- Conversation item.
- Message.
- Search.
- Tag.
- Task.
- User.
- User tag.

Zapier actions:

- Import.
- User.
- Message.
- User tag.

Zapier triggers:

- Message received.
- First message received.
- Task completed.
- Task failed.
- User accepted.
- User tag.
- Web visit.
- First web visit.

### Evolut Cloud Functions

Repo: `linkedin_cloud_functions`.

Function:

- `resize-image`: GCS-triggered image resize function.

Packages:

- `@google-cloud/storage`
- `child-process-promise`

Behavior:

- Ignores non-image files.
- Ignores thumbnail files prefixed with `thumb_`.
- Generates `thumb_200_` and `thumb_400_` images with ImageMagick `convert`.
- Uploads thumbnails back into the same bucket.

### Evolut Older API

Repo: `linkedin_api_old`.

Purpose:

- Older TypeScript/Express API before the newer NestJS `linkedin_api`.

Technologies:

- Express.
- TypeScript.
- Sequelize 4.
- MySQL.
- Socket.IO.
- Firebase Admin.
- JWT auth and `socketio-jwt`.
- Stripe.
- SendGrid.
- Google Cloud Language, Pub/Sub, Storage.
- Jade/Hogan templates.
- App Engine flexible custom runtime.
- Docker Node `10.7.0`.

### Evolut Cloud and Deployment Summary

Google Cloud components found in source:

- App Engine Flexible services:
  - `default` for frontend.
  - `api` for API.
  - `bot` for bot.
- GKE clusters/deployments:
  - `frontend` cluster/deployment `frontend-deploy`.
  - `api` cluster/deployment `api-deploy`.
  - `bot` cluster/deployment `bot-deploy`.
- Google Container Registry images:
  - `gcr.io/$PROJECT_ID/frontend`
  - `gcr.io/$PROJECT_ID/api`
  - `gcr.io/linkedin-207520/bot`
- Cloud Build.
- Cloud KMS for decrypting application credentials.
- Google Cloud Storage:
  - configuration bucket `configuration-linkedin`.
  - release bucket `linkedin-release`.
  - image buckets such as `company-picture` and `user-picture` in older API code.
- Google Cloud Pub/Sub task/progress/webhook queues.
- Google Cloud DNS package used from API.
- Google Cloud Logging with Bunyan.
- Google Cloud Language.
- Firebase Auth/Realtime Database/Storage project `linkedin-207520`.
- Kubernetes ingress-nginx.
- cert-manager and LetsEncrypt certificate manifests.
- Domains: `evolut.net`, `www.evolut.net`, `portal.evolut.net`, `api.evolut.net`, `api.beta.evolut.net`.
- Dispatch routing in `linkedin_default/dispatch.yaml` maps `api.evolut.net/*` to `api` and `www.evolut.net/*` to `default`.

### Evolut CI/CD Summary

- Cloud Build builds Docker images for frontend, API, bot, and downloadable package workflows.
- Frontend/API/bot images are tagged with short SHA and latest.
- Cloud Build updates Kubernetes deployments with `kubectl set image`.
- API Cloud Build starts Docker Compose with API and DB, then runs production tests before deployment.
- Downloadable Cloud Build decrypts app credentials and runs the deploy script.
- Local development compose in `linkedin_default` runs frontend, API, and bot with mounted repos and Google application credentials.
- Shared Bit components require registry setup with `npm config set '@bit:registry'`.
- Node version constraints are important:
  - API Docker uses Node `12.4.0`.
  - Frontend Docker uses Node `12.13.0`.
  - Old API Docker uses Node `10.7.0`.
  - Bot README says Node 8 was required for some node-pre-gyp constraints, while Bit did not run on Node 8.
  - Downloadable app uses Electron 4/Node 10 compatibility to avoid node-gyp issues.

## Project: Zap

### One-Line Summary

Zap is a Go CLI for tipping open-source maintainers with Bitcoin/Lightning. It connects to a wallet through Nostr Wallet Connect, discovers maintainers through GitHub/package metadata, supports LNURL/lightning-gift flows, can send gift redemption emails through SMTP, and ships through a Homebrew tap.

### Project Skills

- Go, Cobra CLI, Bubble Tea, Lipgloss, Nostr Wallet Connect, LNURL, Nostr, Lightning, GitHub API, package-manager metadata parsing, SMTP email, Homebrew formula distribution, GoReleaser-style release artifacts, CLI UX, local credential/config handling.

### Repositories

- `sudonym-btc/zap`
- `sudonym-btc/homebrew-zap`

### Product Scope

Zap is intended for developers to install locally and use while working. During development, it can detect package-install commands and prompt the developer to tip maintainers of dependencies. If a maintainer has a lightning-enabled address, Zap can send a tip directly. If not, it can create a Lightning gift and send an email with a redemption link when SMTP is configured.

Source-backed capabilities:

- Connects to wallets that support Nostr Wallet Connect.
- Supports commands such as connecting a wallet, tipping a GitHub organization, tipping an email address, and manual confirmation flows.
- Includes package analyzer modules for ecosystems such as npm, yarn, pip, Maven, Gradle, Brew, apt, Go, Cargo, pods, and ports.
- Uses GitHub manager code to inspect repository/maintainer context.
- Includes email service support for gift-redemption emails.
- Warns that wallet credentials are stored obscured but not encrypted, and recommends minimal wallet spending permissions.
- `homebrew-zap` contains a generated Homebrew formula for macOS ARM and Intel releases.

### Architecture and Packages

Go module: `github.com/sudonym-btc/zap`.

Runtime packages include:

- `github.com/spf13/cobra` for CLI commands.
- `github.com/charmbracelet/bubbletea`, `bubbles`, and `lipgloss` for terminal UI.
- `github.com/nbd-wtf/go-nostr` for Nostr.
- `github.com/fiatjaf/go-lnurl` for LNURL.
- `github.com/google/go-github` for GitHub integration.
- `gopkg.in/mail.v2` for SMTP email.
- LND/Lightning-related Go dependencies through wallet and invoice flows.

Main source areas:

- `cmd`: CLI command definitions.
- `service/wallet.go`: wallet connection/payment flow.
- `service/github-manager`: GitHub organization/repository lookup.
- `service/package-analyzer`: package-manager ecosystem analyzers.
- `service/lightning-gifts`: Lightning gift handling.
- `service/email`: SMTP email sending.
- `service/tip`: tip orchestration.
- `service/config`: local config/credential handling.

## Project: Portfolio Site

Repository: `/Users/sudonym/Desktop/pats2sats.github.io`.

Public URL: https://pats2sats.github.io/

Purpose:

- One-page GitHub Pages portfolio for Patrick Geyer.
- Inspired by the style and interaction pattern of https://www.pszostak.pl/.
- Features Hostr, Evolut, Sudonym, and Nestr.
- Contact email: `patrick.geyer1@gmail.com`.
- Uses Patrick's GitHub avatar.
- Uses real project visuals where available.

Current stack:

- Vite `8`.
- React `19`.
- TypeScript `~6.0`.
- `lucide-react`.
- CSS custom styling in `src/styles.css`.

Scripts:

- `npm run dev`
- `npm run build`
- `npm run preview`

GitHub Pages workflow:

- `.github/workflows/deploy.yml`
- Runs on push to `main` and manual dispatch.
- Uses Node `22`.
- Runs `npm ci`.
- Runs `npm run build`.
- Uploads `dist`.
- Deploys with `actions/deploy-pages`.

Current visual assets:

- Hostr screenshot from local Hostr working directory.
- Hostr product demo video at `/assets/hostr-product-demo.mp4`.
- Nestr screenshot from local Nestr app.
- Evolut screenshot extracted from demo video.
- Evolut demo opens as a Google Drive preview player because the source video is a 2.2 GB Drive file named `Evolut Demo.mov`.
- Sudonym screenshot from the Sudonym homepage assets.
- Sudonym demo video at `/assets/sudonym-linkedin-demo.mp4`, sourced from Patrick's LinkedIn "bank/wallet app" post: `https://www.linkedin.com/posts/patrick-geyer-5489b660_releasing-a-bankwallet-app-very-soon-such-activity-7003216362433040384-zjgv`.
- GitHub avatar from GitHub avatar CDN.

Featured project repo visibility in the portfolio:

- Hostr: public repo, `https://github.com/sudonym-btc/hostr`.
- Evolut: private source repos under `pats2sats`, represented by `https://github.com/pats2sats/linkedin_frontend`.
- Sudonym: private repo, `https://github.com/sudonym-btc/sudonym`.
- Nestr: public repo, `https://github.com/sudonym-btc/nestr`.

## AI Interaction Plan for Portfolio

The original inspiration site used a custom `/api/chat` endpoint with streamed OpenAI responses, likely through Vercel AI SDK/OpenAI, not a simple embeddable plugin. A static GitHub Pages site cannot safely call OpenAI directly with Patrick's API key because browser-side keys are visible to users.

Practical options:

- Use a hosted chatbot widget such as Chatbase, SiteGPT, DocsBot, or similar. Train it on this `profile.md`, project repos/docs, and selected public pages. This avoids hosting Patrick's own backend.
- Use a user-supplied API key in the browser for local/private demos only. Do not use this for a public portfolio.
- Use a tiny serverless proxy only if a custom chat UI and exact styling are required.

Recommended no-backend setup:

1. Publish `public/profile.md` so it is reachable at `https://pats2sats.github.io/profile.md`.
2. Create a Chatbase/SiteGPT/DocsBot bot.
3. Add the public profile URL and any selected docs URLs as sources.
4. Paste the widget embed into the portfolio.
5. Configure the bot to answer as a portfolio assistant for Patrick and to cite uncertainty.

## Interview Gaps for Patrick

Ask Patrick these follow-up questions before treating this profile as final:

1. What title should the AI use for Patrick today: "Senior Full-Stack Engineer", "Product Engineer", "Founder/CTO", "Bitcoin/Nostr Engineer", or another phrase?
2. Should the AI mention Patrick's phone number from the resume, or keep contact to email only?
3. Should location be described as London/Munich/San Salvador, remote/LATAM/Europe, or something else?
4. Which current roles or contract types should the AI say Patrick is open to?
5. For Hostr, what is the most accurate status: prototype, alpha, staging, production, app-store ready, or shipping?
6. For Nestr, should it be described as a prototype, hack/demo, open-source experiment, or product direction?
7. For Evolut, should the AI describe it as discontinued, past startup, archived codebase, or portfolio case study?
8. For Sudonym, should the AI describe it as archived, demo, App Store/TestFlight experience, or a past startup product?
9. For NMDK and the marketplace packages, should the AI describe them as active protocol/R&D work, production-bound libraries, or supporting infrastructure for Hostr?
10. For Zap, should the AI describe it as a public developer tool, experiment, archived project, or active product direction?
11. May the AI mention private GitHub repo names like `linkedin_api`, `linkedin_bot`, and `sudonym-btc/sudonym`, or should those be summarized without repo names?
12. Which Evolut business outcomes can be stated safely: customers, revenue, conversion improvements, usage volume, or "ran a SaaS business" only?
13. Which Sudonym business/product outcomes can be stated safely: app-store release, user count, transaction volume, Lightning node operations, or "launched a custodial Bitcoin wallet" only?
14. Are there any technologies Patrick does not want emphasized because they are old or no longer representative?
15. Should the portfolio AI answer in first person as Patrick, or third person as Patrick's assistant?
16. What tone should the AI use: concise recruiter-friendly, technical/deep, playful, or balanced?
17. Should the AI mention Bitcoin/crypto prominently for all audiences, or adapt based on the visitor's question?
18. Are there any companies, client names, credentials, screenshots, or project details that should be treated as private?
19. Which hosted chatbot provider should be wired into the portfolio first?

## Safe Claims

The AI can safely say:

- Patrick has 10+ years of JavaScript/Node.js experience.
- Patrick is backend-leaning but works full stack.
- Patrick has substantial TypeScript/NestJS experience.
- Patrick has built production systems on Google Cloud and AWS.
- Patrick has deployed systems with Docker, CI/CD, Terraform, Cloud Build, GitHub Actions, GCP Artifact Registry, App Engine/GKE, and app-store pipelines.
- Patrick has built or prototyped products involving Bitcoin, Lightning, LNURL, Nostr, Cashu, Rootstock/EVM, account abstraction, Flutter, Dart, React, Angular, Node.js, NestJS, Go, and TypeScript.
- Hostr is a Nostr-based short-term rental marketplace prototype with Flutter, Dart, Terraform/GCP, Docker, Nostr relay services, Blossom, Lightning/Bitcoin local stacks, EVM escrow contracts, and MCP/AI automation.
- NMDK is a Nostr marketplace development kit/project family with TypeScript protocol packages, EVM escrow/payment drivers, Cashu escrow/payment drivers, H3 location packages, Solidity contract artifacts, local Docker regtest stacks, a React marketplace demo app, and generated documentation.
- The NMDK marketplace stack includes experience with Cashu, P2PK escrow proofs, Boltz swap orchestration, ERC-4337 account abstraction, viem, permissionless, Anvil, Rootstock/Arbitrum-style local chains, nostr-rs-relay, LND, LNbits, Alby Hub, Blossom, TypeDoc, Fumadocs, and GitHub Pages docs publishing.
- Nestr is a virtual office environment for nostr protocol chatrooms using React, TypeScript, Three.js, NIP-29, NIP-46, NIP-42, WebRTC concepts, encrypted IndexedDB session storage, and deterministic 3D rooms.
- Evolut was a LinkedIn marketing automation SaaS built with Angular, NestJS/Node, Puppeteer, MySQL/Sequelize, Firebase, GCP, Docker, Cloud Build, Kubernetes/App Engine, Stripe, SendGrid, Zapier, and Electron.
- Sudonym was a white-label Bitcoin and Lightning wallet built with Flutter, Dart, NestJS, TypeScript, MySQL/Sequelize, Firebase, Core Lightning, c-lightning-rest, LNURL, BOLT card/NFC support, Google Cloud Deployment Manager, GKE, Cloud Build, and Codemagic app-store pipelines.
- Zap is a Go CLI for Bitcoin/Lightning maintainer tipping with Nostr Wallet Connect, LNURL, GitHub/package metadata analysis, SMTP-based lightning-gift emails, terminal UI packages, and Homebrew distribution.

## Claims That Need Confirmation

Do not state these as fact until Patrick confirms:

- Current job-seeking status.
- Current location and relocation/remote preferences.
- Exact current availability.
- Revenue/customer metrics for Evolut.
- Hostr production/commercial status.
- Nestr product roadmap.
- Sudonym current availability, user/transaction metrics, and whether it should be framed as archived, demo, or launched product.
- NMDK package status, production-readiness, release/versioning plans, and whether it should be framed as Hostr infrastructure or standalone marketplace protocol work.
- Zap current maintenance status, usage metrics, release status, and whether wallet credential handling has changed since the inspected README warning.
- Whether private repo names may be mentioned publicly.
- Whether phone number should be exposed.
- Whether older Evolut details should be framed as "legacy stack" or simply "historical startup work."
